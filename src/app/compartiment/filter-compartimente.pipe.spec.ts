import { FilterCompartimentePipe } from './filter-compartimente.pipe';

describe('FilterCompartimentePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCompartimentePipe();
    expect(pipe).toBeTruthy();
  });
});
